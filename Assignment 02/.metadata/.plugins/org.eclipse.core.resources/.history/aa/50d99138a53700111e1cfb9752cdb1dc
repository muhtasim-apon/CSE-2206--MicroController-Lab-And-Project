#include "stm32f446xx.h"

int main(void)
{
    RCC->AHB1ENR |= RCC_AHB1ENR_GPIOAEN;

    GPIOA->MODER &= ~(3UL<<12);
    GPIOA->MODER |= (1UL<<12); // PA6 output

    GPIOA->ODR |= (1<<6); // HIGH

    while(1);
}
